@model SolicitudesMVC.Models.DetalleSolicitud

@{
    ViewData["Title"] = "Create";
}
@*
<h1>Create</h1>
*@
<h6>DetalleSolicitud</h6>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdCurso" class="control-label"></label>
                <select asp-for="IdCurso" class="form-control" asp-items="ViewBag.Curso"></select>
            </div>
           <div class="form-group">
                <label asp-for="Profesor" class="control-label">Alumno</label>
                <select asp-for="Profesor" class="form-control" asp-items="ViewBag.Nombre"></select>
            </div>
            <div class="form-group">
                <label asp-for="Aula" class="control-label">Aula</label>
                <select asp-for="Aula" class="form-control" asp-items="ViewBag.Nombre"></select>
            </div>
            <div class="form-group">
                <label asp-for="Sede" class="control-label">Sede</label>
                <select asp-for="Sede" class="form-control" asp-items="ViewBag.Nombre"></select>
            </div>
            <div class="form-group">
                <label asp-for="Observación" class="control-label"></label>
                <input asp-for="Observación" class="form-control" />
                <span asp-validation-for="Observación" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
